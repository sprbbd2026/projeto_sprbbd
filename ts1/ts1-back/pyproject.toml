[project]
name = "ts1-back"
version = "0.1.0"
description = "BFF do TS1 — cadastro de usuário no Supabase (service role, sem RPC no browser)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic[email]>=2.10.0",
    "pydantic-settings>=2.6.0",
    "python-dotenv>=1.0.0",
    "supabase>=2.10.0",
    "slowapi>=0.1.9",
]

[tool.uv]
package = false
